---
title:
layout: page
permalink: /index.html
eleventyExcludeFromCollections: true
---

{% assign lastTen = collections.weblog | reverse | limit:10 %}

{%- for post in lastTen-%}
    <article class="home-article">
        <p class="date home-date">{% readable-date post.data.date %}</p>

        <h3 class="links"><a href="{{ post.url }}">{{ post.data.title }}</a></h3>

        <p class="intro">{{ post.content | strip_html | truncate: 500, "…" }}</p>
    </article>
{%- endfor -%}


